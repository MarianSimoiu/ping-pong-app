// Shared app constants. Mirrors the rating rules documented in docs/RATING.md.

// A player must have played at least this many matches to appear on the public
// leaderboard (matches the provisional threshold in the rating engine).
export const MIN_MATCHES_FOR_LEADERBOARD = 5;
